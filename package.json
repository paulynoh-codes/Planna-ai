{
  "name": "planna-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Planna — social travel planning app (MVP).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,mobile -c blue,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/mobile\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:mobile": "npm run dev -w apps/mobile",
    "build": "npm run typecheck",
    "test": "npm run test -w apps/api",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/api && npm run typecheck -w apps/mobile"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
